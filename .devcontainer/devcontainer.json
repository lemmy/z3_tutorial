{
	"name": "z3 Tutorial",

	// Add the IDs of extensions you want installed when the container is created.
	"extensions": [
		"ms-toolsai.jupyter",
        "ms-python.python",
		"shd101wyy.markdown-preview-enhanced"
	],

	"postCreateCommand": "sudo apt-get update && sudo apt-get install python3 python3-venv python3-pip -y && pip install z3-solver"
}